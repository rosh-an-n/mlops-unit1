# mlops-unit1

Simple MLOps practice project.

## Setup

```bash
python -m venv venv
source venv/bin/activate
pip install -r requirements.txt
```

## Run

```bash
python src/data_stats.py
python src/train.py
```

The trained model gets saved in `models/model.pkl`.
